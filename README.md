backend: node.js + expressJS + typescript + typeORM + inversifyJS (IoC/DI)

frontend: react + flow

auth: passport.js

api: grapgql (type-grapghql)

db: postgresql + redis

deploy: docker + pm2